name: RDP / Tailscale / RustDesk - PC A

on:
  workflow_dispatch:
    inputs:
      authkey:
        description: "Tailscale Auth Key"
        required: true
        type: string

jobs:
  connect-pc-a:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # 🔐 Generate PC Name + Random Password
      - name: Generate PC Credentials
        id: gen
        run: |
          $pcName = "PC-A-" + (Get-Random -Maximum 9999)
          $password = [System.Web.Security.Membership]::GeneratePassword(12,3)
          echo "::set-output name=pcName::$pcName"
          echo "::set-output name=password::$password"

      - name: Show Generated Credentials (Visible in Job Summary)
        run: |
          Write-Host "PC Name: ${{ steps.gen.outputs.pcName }}"
          Write-Host "Generated Password: ${{ steps.gen.outputs.password }}"

      # 🟢 Install Tailscale
      - name: Install Tailscale
        run: |
          choco install tailscale -y
          refreshenv

      # 🟢 Verify Tailscale
      - name: Verify Tailscale
        shell: powershell
        run: |
          $ts = "C:\Program Files\Tailscale\tailscale.exe"
          if (Test-Path $ts) { & $ts version } else { Exit 1 }

      # 🟢 Connect to Tailscale (use generated hostname)
      - name: Connect Tailscale
        run: |
          & "C:\Program Files\Tailscale\tailscale.exe" up `
            --authkey="${{ github.event.inputs.authkey }}" `
            --hostname="${{ steps.gen.outputs.pcName }}"

      # 🟢 Install RustDesk IF missing
      - name: Install RustDesk
        shell: powershell
        run: |
          $rd = "C:\Program Files\RustDesk\rustdesk.exe"
          if (-Not (Test-Path $rd)) {
              choco install rustdesk -y
              refreshenv
          }

      # 🟢 Launch RustDesk
      - name: Run RustDesk
        run: |
          Start-Process "C:\Program Files\RustDesk\rustdesk.exe"

      - name: Done
        run: echo "PC-A connected via Tailscale & RustDesk started."
